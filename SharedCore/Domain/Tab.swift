import Foundation

enum Tab: String, Codable, Sendable {
    case home = "Home"
    case plan = "Plan"
    case search = "Search"
    case goals = "Goals"
    case timeline = "Timeline"
    case stats = "Stats"
    case settings = "Settings"
}
